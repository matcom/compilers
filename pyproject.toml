[project]
name = "compilers"
version = "0.1.0"
description = "Course materials for Introduction to Compiler Construction"
requires-python = ">=3.10"
dependencies = [
    "pydot",
    "nbformat",
    "nbclient",
    "jupyter",
]

[tool.uv]
dev-dependencies = []
