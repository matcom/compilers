let sum = 0 in {
    let i = 0 in
        while (i < 3) {
            let j = 0 in
                while (j < 3) {
                    sum := sum + 1;
                    j := j + 1;
                };
            i := i + 1;
        };
    if (sum == 9) print("ok") else print("fail");
};
