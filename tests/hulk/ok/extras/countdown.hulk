{
    let i = 5 in {
        while (i > 0) {
            i := i - 1;
        };
        if (i == 0) print("ok") else print("fail");
    };
    let j = 0 in {
        while (j < 10) {
            j := j + 3;
        };
        if (j == 12) print("ok") else print("fail");
    };
}
